/// <reference types="axios" />

declare module 'axios' {
  // This file ensures TypeScript can find the axios module during build
  // The actual type augmentations are in individual feature files
}
